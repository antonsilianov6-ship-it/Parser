# Runtime dependencies used by the Docker image.
#
# This file mirrors requirements.txt but pins to versions that *actually*
# resolve on PyPI today. requirements.txt has historically pinned to
# non-existent versions (pandas==3.0.2, cryptography==47.0.0, requests==2.33.1),
# which makes a fresh `pip install` fail. Whenever those upstream packages
# release the pinned versions for real, requirements.txt and this file can be
# reconciled.

# Telegram API
telethon==1.43.2
cryptg==0.6.0

# Google API
google-api-python-client==2.149.0
google-auth==2.35.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.1

# Async I/O
aiofiles==24.1.0

# Data processing
pandas==2.2.3
plotly==5.24.1

# Security
cryptography==43.0.3

# Utilities
schedule==1.2.2
requests==2.32.3
openpyxl==3.1.5

# Proxy support
PySocks==1.7.1
python-socks[asyncio]==2.5.3

# NotebookLM integration
notebooklm-py==0.3.4
